generator client {
  provider = "prisma-client-js"
  output   = "../../../generated/user-service-client"
}

datasource db {
  provider = "postgresql"
}

enum UserRole {
  STUDENT
  PROFESSOR
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  pseudo    String   @unique
  password  String
  role      UserRole @default(STUDENT)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
